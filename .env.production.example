# Copy to .env.production on the ECS host, replace the placeholders, then run
# chmod 600 .env.production. These values initialize the MySQL container.
MYSQL_ROOT_PASSWORD=replace-with-a-different-long-random-root-password
MYSQL_PASSWORD=replace-with-the-same-password-as-server-production-local-yaml
